<UserControl
    x:Class="NetworkService.Views.HomeView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:NetworkService.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="956"
    d:DesignWidth="440"
    mc:Ignorable="d">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="4*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="3*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="1" Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Button
                x:Name="networkEntitiesBtn"
                Grid.Row="0"
                Margin="40"
                Command="{Binding NavCommand}"
                CommandParameter="entities"
                Content="Network Entites" />
            <Button
                x:Name="networkDisplayBtn"
                Grid.Row="1"
                Margin="40"
                Command="{Binding NavCommand}"
                CommandParameter="display"
                Content="Network Display" />
            <Button
                x:Name="measurementGraphBtn"
                Grid.Row="2"
                Margin="40"
                Command="{Binding NavCommand}"
                CommandParameter="graph"
                Content="Measurement Graph" />
        </Grid>
    </Grid>
</UserControl>
