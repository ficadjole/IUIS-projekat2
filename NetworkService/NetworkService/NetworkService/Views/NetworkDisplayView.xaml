<UserControl
    x:Class="NetworkService.Views.NetworkDisplayView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
    xmlns:local="clr-namespace:NetworkService.Views"
    xmlns:local1="clr-namespace:NetworkService.Helpers"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:root="clr-namespace:NetworkService.Model"
    d:DesignHeight="956"
    d:DesignWidth="440"
    mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TextBlock
            Grid.Row="0"
            Margin="10,10,10,0"
            HorizontalAlignment="Center"
            FontSize="20"
            FontWeight="Bold"
            Text="Network Display" />
        <TreeView
            Grid.Row="1"
            Margin="10"
            AllowDrop="True"
            ItemsSource="{Binding SviServeri}">
            <TreeView.Resources>
                <HierarchicalDataTemplate DataType="{x:Type root:ServersByType}" ItemsSource="{Binding Servers}">
                    <StackPanel Orientation="Horizontal">
                        <Image
                            Width="50"
                            Margin="5,0,5,0"
                            VerticalAlignment="Center"
                            Source="{Binding ServerType.UrlImage}" />
                        <TextBlock VerticalAlignment="Center" Text="{Binding ServerType.Type}" />
                        <TextBlock VerticalAlignment="Center" Text=" (" />
                        <TextBlock VerticalAlignment="Center" Text="{Binding Servers.Count}" />
                        <TextBlock VerticalAlignment="Center" Text=")" />
                    </StackPanel>
                </HierarchicalDataTemplate>
                <DataTemplate DataType="{x:Type root:Server}">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding Name}" />
                        <TextBlock Text=" - " />
                        <TextBlock Text="{Binding IpAddress}" />
                    </StackPanel>
                </DataTemplate>
            </TreeView.Resources>

            <i:Interaction.Triggers>
                <i:EventTrigger EventName="SelectedItemChanged">
                    <i:InvokeCommandAction Command="{Binding SelectedItemChangedCommand}" PassEventArgsToCommand="True" />
                </i:EventTrigger>
                <i:EventTrigger EventName="MouseLeftBuutonUp">
                    <i:InvokeCommandAction Command="{Binding MouseLeftButtonUpCommand}" />
                </i:EventTrigger>
                <i:EventTrigger EventName="Drop">
                    <i:InvokeCommandAction Command="{Binding TreeViewDropCommand}" PassEventArgsToCommand="True" />
                </i:EventTrigger>
            </i:Interaction.Triggers>
        </TreeView>

        <ItemsControl
            Grid.Row="2"
            Margin="10"
            ItemsSource="{Binding CanvasSlots}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <UniformGrid
                        Background="Yellow"
                        Columns="4"
                        Rows="3" />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>

            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Canvas
                        Width="80"
                        Height="80"
                        AllowDrop="True"
                        Background="AliceBlue"
                        Tag="{Binding}">
                        <StackPanel Orientation="Vertical">
                            <Image
                                Width="50"
                                Height="50"
                                Source="{Binding Type.UrlImage}" />
                            <StackPanel Orientation="Horizontal">

                                <TextBlock
                                    Canvas.Top="80"
                                    Margin="0,0,5,0"
                                    Text="{Binding Name}" />

                                <TextBlock Canvas.Top="80" Text="{Binding Value}" />
                            </StackPanel>
                        </StackPanel>

                        <i:Interaction.Triggers>
                            <i:EventTrigger EventName="Drop">
                                <i:InvokeCommandAction Command="{Binding DataContext.DropCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}" PassEventArgsToCommand="True" />
                            </i:EventTrigger>
                            <i:EventTrigger EventName="DragOver">
                                <i:InvokeCommandAction Command="{Binding DataContext.DragOverCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}" PassEventArgsToCommand="True" />
                            </i:EventTrigger>
                            <i:EventTrigger EventName="MouseLeftButtonDown">
                                <i:InvokeCommandAction Command="{Binding DataContext.MouseLeftButtonDownCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}" PassEventArgsToCommand="True" />
                            </i:EventTrigger>
                        </i:Interaction.Triggers>
                    </Canvas>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>


    </Grid>
</UserControl>
