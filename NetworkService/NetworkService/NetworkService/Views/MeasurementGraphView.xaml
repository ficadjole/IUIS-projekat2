<UserControl
    x:Class="NetworkService.Views.MeasurementGraphView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:NetworkService.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="956"
    d:DesignWidth="440"
    mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TextBlock
            Grid.Row="0"
            Margin="10,10,10,0"
            HorizontalAlignment="Center"
            FontSize="20"
            FontWeight="Bold"
            Text="Measurement Graph" />
        <StackPanel Grid.Row="1" Margin="10">
            <TextBlock Text="Select one entity" />
            <ComboBox ItemsSource="{Binding Servers}" SelectedItem="{Binding SelectedServer}">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock VerticalAlignment="Center" Text="{Binding Name}" />
                            <Separator Width="5" />
                            <TextBlock VerticalAlignment="Center" Text="{Binding Type.Type}" />
                            <Separator Width="5" />
                            <TextBlock VerticalAlignment="Center" Text="{Binding IpAddress}" />
                        </StackPanel>
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>
        </StackPanel>
        <Grid Grid.Row="2" Margin="10">
            <Canvas
                Width="400"
                Height="650"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Background="LightGray">
                <Line
                    Stroke="Black"
                    StrokeThickness="2"
                    X1="30"
                    X2="30"
                    Y1="0"
                    Y2="630" />
                <Line
                    Stroke="Black"
                    StrokeThickness="2"
                    X1="30"
                    X2="370"
                    Y1="630"
                    Y2="630" />
                <ItemsControl
                    Canvas.Left="10"
                    Canvas.Top="300"
                    ItemsSource="{Binding Podaci}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel Orientation="Horizontal" />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>

                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Vertical">
                                <Rectangle
                                    Width="50"
                                    Height="{Binding Visina}"
                                    Margin="5"
                                    Fill="{Binding Boja}" />
                            </StackPanel>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>


                <Label
                    Canvas.Left="0"
                    Canvas.Top="0"
                    Content="100"
                    FontWeight="Bold"
                    Foreground="Black" />

                <Label
                    Canvas.Left="5"
                    Canvas.Top="150"
                    Content="75"
                    FontWeight="Bold"
                    Foreground="Black" />

                <Label
                    Canvas.Left="5"
                    Canvas.Top="310"
                    Content="50"
                    FontWeight="Bold"
                    Foreground="Black" />

                <Label
                    Canvas.Left="5"
                    Canvas.Top="460"
                    Content="25"
                    FontWeight="Bold"
                    Foreground="Black" />

                <Label
                    Canvas.Left="5"
                    Canvas.Top="615"
                    Content="0"
                    FontWeight="Bold"
                    Foreground="Black" />

                <ItemsControl
                    Canvas.Left="50"
                    Canvas.Top="625"
                    ItemsSource="{Binding Podaci}">

                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel Orientation="Horizontal" />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>

                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Label
                                Width="50"
                                Margin="5"
                                HorizontalContentAlignment="Center"
                                Content="{Binding Vreme}"
                                FontWeight="Bold"
                                Foreground="Black" />
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </Canvas>
        </Grid>
    </Grid>
</UserControl>
